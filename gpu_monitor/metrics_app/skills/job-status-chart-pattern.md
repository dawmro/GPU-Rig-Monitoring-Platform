name: gpu-rig-chart-route-pattern
description: Chart route pattern for MetricSnapshot timeseries metrics. Add metric to SNAPSHOT_METRICS, serializer defaults, model field, migration, compact agg_fields, then chart card + loader registry.
files:
  - references/job-status-chart-plan.md
