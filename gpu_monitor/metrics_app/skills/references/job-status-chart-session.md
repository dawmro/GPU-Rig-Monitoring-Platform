# Job Status Chart — Session Reference (2026-09-10)

Plan branch: `plan/chart-job-status` → feature: `feat/chart-job-status`.
Plan file: `PLAN_chart_job_status.md` (updated with corrected compaction/cleanup reality and 6-independent-steps A-F + Step G chart display).

## Verified code changes (commits on feat/chart-job-status)
- A (`01ae046`): `0051_metric_snapshot_has_active_job.py` — model field.
- B (`f236afa`): serializer `defaults` writes `has_active_job`.
- C (`de00a99`): `compact_data.py` `agg_fields`: `'has_active_job': 'avg'`.
- D (`8073995`): `SNAPSHOT_METRICS` includes metric.
- E (`9673094`): `checks.py` (E001-E004 defense layers).
- F (`9673094` same): `skills/job-status-chart-pattern.md` + `references/job-status-chart-plan.md`.
- G (`2b8130c`): `rig_detail.html` card + `chart-runtime.js` loader (gold/yellow, `unit=''`).

## Regression discovered and fixed
- `0052_alter_metricsnapshot_has_active_job` appeared (auto-generated by `sync_to_opt.sh` `makemigrations` when `models.py` had `null=True` without `blank=True`). Deleted; fixed model (`blank=True` added); added removal guard to `sync_to_opt.sh`.
- `0052_remove_...` appeared (same cause, opposite operation). Deleted; same fix.

## Empty-chart diagnosis (empirical, not guess)
Screenshot shows `Job Status (Active %)` card rendered but empty. DB verified (via `.env` credentials):
- `metrics_metricsnapshot`: 11,506 rows; column `has_active_job` exists; 11,506 non-null; latest = `True`.
- 24h SQL aggregation (`AVG(has_active_job::int)`): `avg = 1.0`, `count = 2587` per bucket.
- Code pipeline verified (model, serializer, view, loader, template, compaction, checks, `/opt` sync match).
- Root cause: gunicorn server hasn't restarted since feature deploy (`/opt` files updated but running process serves cached Python). Fix: `systemctl restart gunicorn` or `bash scripts/sync_to_opt.sh` (includes restart at line 255).

Not a data bug, not a missing-field bug — solely a deployment/state gap.
